function syncAdd(a, b) {
    return a + b;
  }
  
  function syncSubtract(a, b) {
    return a - b;
  }
  
  
  console.log("Sync Add:", syncAdd(3, 3));
  console.log("Sync Subtract:", syncSubtract(5, 4));